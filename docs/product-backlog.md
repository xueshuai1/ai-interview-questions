# 📋 产品待办清单
> 生成时间：2026-04-11 23:05 (Asia/Shanghai)
> 基于：content-report + qa-report + 知识库内容深度检查 + 实际网站体验

---

## 📊 当前产品状态

### 优势（继续保持）
- ✅ 导航结构完整（首页/知识库/工具集/博客/学习路线/关于），所有页面可访问
- ✅ 知识库搜索和分类筛选功能正常
- ✅ 整体视觉设计统一，暗色主题质感不错
- ✅ 响应式布局（PC + 移动端）基本正常
- ✅ 有 2 篇文章（Transformer、LLM训练）在数据层已写了高质量内容（代码+表格+Mermaid图）

### 短板（需要改进）
- 🔴 **文章内容渲染器是假的** — `generateContent()` 函数按分类生成通用模板文本，完全无视 `article.content` 字段！所有 19 篇文章内容完全一样空洞
- 🔴 **17/19 篇文章只有 summary 没有 content** — 知识库是个空壳
- 🔴 知识库首行卡片对比度极低（深色卡片在深色背景上几乎不可读）
- 🟡 博客卡片不可点击、新闻卡片不可点击、Footer 全是死链
- 🟡 首页导航与其他页面不一致（缺少博客、学习路线链接）
- 🟡 控制台 /blog 和 /roadmap 有 404 预取错误

### 机会（值得探索）
- 内容研究发现了 5 个热点话题（Claude Mythos、Nvidia GTC、Cursor 3、GLM-5.1、AI功能性情感），可以作为博客内容
- 竞品（DeepLearning.AI、MDN）的内容深度远超我们，差距巨大但也意味着提升空间巨大
- `ArticleContent` 组件已经写好（支持代码高亮、Mermaid、表格、TipBox），只需要被正确调用

---

## 📝 知识库文章内容质量报告

### 有实质内容的文章：2/19（10.5%）
| ID | 标题 | 内容深度 |
|----|------|---------|
| `dl-004` | 注意力机制与 Transformer 架构 | ✅ 7 个章节，含代码、Mermaid、表格 |
| `llm-001` | 大语言模型训练全流程 | ✅ 7 个章节，含代码、Mermaid、表格 |

### 只有 summary 的文章：17/19（89.5%）
| ID | 标题 | 标称阅读时长 | 实际内容 |
|----|------|------------|---------|
| `ml-001` | 线性回归：机器学习的第一课 | 8 min | 只有一行 summary |
| `ml-002` | 决策树与随机森林实战 | 12 min | 只有一行 summary |
| `ml-003` | SVM 支持向量机详解 | 15 min | 只有一行 summary |
| `ml-004` | K-Means 聚类算法深入剖析 | 10 min | 只有一行 summary |
| `ml-005` | XGBoost 原理与调参指南 | 18 min | 只有一行 summary |
| `dl-001` | 神经网络基础：从感知机到多层网络 | 12 min | 只有一行 summary |
| `dl-002` | CNN 卷积神经网络完全指南 | 20 min | 只有一行 summary |
| `dl-003` | RNN 与 LSTM：处理序列数据 | 15 min | 只有一行 summary |
| `dl-005` | GAN 生成对抗网络原理与应用 | 16 min | 只有一行 summary |
| `nlp-001` | 词嵌入：从 Word2Vec 到 GloVe | 12 min | 只有一行 summary |
| `nlp-002` | BERT 预训练模型深度解析 | 18 min | 只有一行 summary |
| `cv-001` | 目标检测：从 R-CNN 到 YOLO | 20 min | 只有一行 summary |
| `cv-002` | 图像分割：语义分割与实例分割 | 16 min | 只有一行 summary |
| `llm-002` | Prompt Engineering 最佳实践 | 15 min | 只有一行 summary |
| `llm-003` | RAG 检索增强生成架构指南 | 18 min | 只有一行 summary |
| `agent-001` | AI Agent 入门：从概念到实现 | 15 min | 只有一行 summary |
| `agent-002` | Multi-Agent 系统设计与协作 | 20 min | 只有一行 summary |

### ⚠️ 额外问题：文章内容渲染 Bug
即使 `dl-004` 和 `llm-001` 有完整的 `content` 数据，文章页面也没有渲染它们！
`page.tsx` 中的 `generateContent()` 函数完全无视 `article.content`，按分类生成通用模板。
`ArticleContent` 组件已存在但从未被导入使用。

### 需要立即填充的文章（优先级排序）
1. `llm-002` Prompt Engineering — 最实用、搜索量最大的话题
2. `llm-003` RAG 架构 — 企业应用最热门
3. `agent-001` AI Agent 入门 — 当前最大热点
4. `dl-001` 神经网络基础 — 入门流量入口
5. `ml-001` 线性回归 — 机器学习入门第一课

---

## 🎯 本周重点方向（2 个）

### 方向 1：修复文章内容渲染器（P0）
**原因**：这是整个网站的核心价值。文章页面渲染假内容 = 用户在知识库看不到任何有价值的东西。修复后，已有的 2 篇高质量文章才能展示，后续填充的内容才有意义。

### 方向 2：填充 3 篇核心文章内容（P0）
**原因**：19 篇里只有 2 篇有内容，知识库是个空壳。优先填充搜索量最大、最实用的 3 篇文章，让网站看起来"真的有东西"。

---

## 📝 待办清单（按优先级排序）

### P0 - 必须做（本周内完成）

#### #1 修复文章内容渲染 Bug
- **问题**：`/article/[id]/page.tsx` 的 `generateContent()` 无视 `article.content`，生成通用模板
- **需要做的**：
  1. 在文章页面中导入 `ArticleContent` 组件
  2. 当 `article.content` 存在时，用 `ArticleContent` 渲染真实内容
  3. 当 `article.content` 不存在时，才使用 `generateContent()` 作为 fallback（但应标记为"内容填充中"）
  4. 删除或标记 `generateContent()` 为临时方案
- **预计工作量**：30 分钟
- **影响范围**：19 篇文章

#### #2 修复知识库首行卡片对比度（QA P0）
- **问题**：知识库页面第一行（ML/深度学习/NLP）卡片背景色在深色页面上几乎不可读
- **需要做的**：检查 ArticleCard 组件样式，确保卡片背景色与页面背景有足够对比度
- **预计工作量**：15 分钟

#### #3 填充 3 篇核心文章的 content
- **优先级排序**：
  1. `llm-002` Prompt Engineering 最佳实践（入门级，流量最大）
  2. `llm-003` RAG 检索增强生成架构指南（进阶级，实用性强）
  3. `agent-001` AI Agent 入门：从概念到实现（当前最热话题）
- **每篇文章要求**：至少 5-7 个章节，包含代码示例、表格或 Mermaid 图
- **对标标准**：维基百科级别深度，不是一两句话就标 "15 min read"
- **预计工作量**：每篇约 1-2 小时（AI 生成内容需要人工审核质量）

### P1 - 应该做（下周）

#### #4 博客卡片添加点击交互 + 详情页面
- 博客卡片目前不可点击，缺少文章详情页
- 需要添加 `/blog/[id]` 路由或改为点击跳转

#### #5 首页导航统一化
- 首页使用内联导航（只有 3 个链接），其他页面用 Navbar 组件（7 个链接）
- 首页应改用 Navbar 组件保持一致性

#### #6 Footer 链接修复
- 所有 Footer 链接指向 `#`，需要改为实际页面链接

#### #7 修复 /blog 和 /roadmap 的 404 预取错误
- Next.js RSC 预取请求 404，将页面改为 RSC 或调整预取策略

### P2 - 可以做（有空再做）

#### #8 首页新闻卡片添加可点击链接
- 新闻卡片目前是纯展示，可以链接到对应的博客文章

#### #9 博客分页功能
- 分页按钮是纯视觉占位，添加实际分页逻辑

#### #10 博客分类筛选
- 添加按分类筛选博客文章的功能

---

## 🚫 本周不做
- ❌ 新增「AI 安全」专题频道 — 内容都没有填完，不要加新板块
- ❌ 价格计算器功能 — 先有内容，再有工具
- ❌ AI 新闻时间线组件 — RSS 接入是后期优化
- ❌ SEO 优化 — 内容质量是 SEO 的前提，现在做 SEO 是给空壳做推广
- ❌ 博客文章详情内容填充 — 先把知识库的 19 篇文章填满再说
- ❌ 移动端加载优化 — 当前的主要问题不是性能，是内容

---

## 💡 给开发 Agent 的建议

**建议你下次运行优先做 #1（修复文章渲染 Bug）**，原因：

1. 这是**阻塞性问题** — 不修好它，后续填充的所有文章内容都看不到
2. **改动量最小** — 只需要在 article page 导入 ArticleContent 组件，加个条件渲染，30 分钟搞定
3. **立竿见影** — 修完后 dl-004 和 llm-001 两篇高质量文章立刻可以展示
4. 做完 #1 后立即做 #2（对比度修复），然后启动 #3（填充 3 篇文章内容）

**执行顺序建议**：
```
#1 修复渲染（30min）→ #2 修复对比度（15min）→ #3 填充 3 篇文章内容（3-6h）
```

做完这三件事，网站从"空壳"变成"至少有几篇真正有价值的文章的学习平台"。

---

## 📊 决策依据

| 数据点 | 数值 |
|--------|------|
| 知识库文章总数 | 19 |
| 有实质内容的文章 | 2 (10.5%) |
| 只有 summary 的文章 | 17 (89.5%) |
| QA 发现问题总数 | 8 |
| QA P0 问题 | 1 |
| QA P1 问题 | 3 |
| QA P2 问题 | 4 |
| 文章页面渲染 Bug | 1（影响全部 19 篇文章）|
| 热点话题待转化 | 5 个（Claude Mythos, Nvidia GTC, Cursor 3, GLM-5.1, AI情感研究）|

---

## 🔄 自我改进记录

### ERRORS.md 更新
- 上次运行没有 backlog 文件（docs/ 目录为空），说明第一次运行没有正确生成或文件被删除
- 本次确保覆盖写入到正确路径

### LEARNINGS.md 更新
- 发现 `generateContent()` 函数是一个"看起来在工作"的陷阱 — 它确实生成了内容，但是假内容。以后做代码审查要特别警惕这种"看似合理但实际无效"的代码
- 文章数据层 (`knowledge.ts`) 和渲染层 (`page.tsx`) 的脱配是典型的数据驱动应用错误 — 定义了数据但没有消费它
- `ArticleContent` 组件已经写好但从未使用，说明开发流程中缺少"组件使用检查"环节

### FEATURE_REQUESTS.md 更新
- 建议在知识库文章列表中增加"内容完整度"标识（有 content 的显示 ✅，只有 summary 的显示 🔄 填充中）
- 建议增加文章字数/章节数统计，防止"8 min read"但实际上只有一行文字的情况
