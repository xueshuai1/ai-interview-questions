#!/usr/bin/env node
// Replace ASCII diagrams with simple lists in all guide articles
import { readFileSync, writeFileSync } from 'fs';

const DIR = 'src/data/articles';

const replacements = {
  'prompt-guide.ts': {
    search: `以下是 Prompt Engineering 的完整学习路径：

\`\`\`
┌─ 第 1 步：认识 Prompt ──┐
│ • 什么是 Prompt        │
│ • 常见错误和反模式     │
├────────────────────────┤
│ 第 2 步：结构化提示词  │
│ • 四段式写法           │
│ • 模板化思维           │
├────────────────────────┤
│ 第 3 步：进阶技巧      │
│ • CoT 思维链           │
│ • Few-shot 示例        │
│ • ReAct 推理-行动模式  │
└────────────────────────┘
\`\`\``,
    replace: `以下是 Prompt Engineering 的完整学习路径：

**第 1 步：认识 Prompt**
- 什么是 Prompt
- 常见错误和反模式

**第 2 步：结构化提示词**
- 四段式写法
- 模板化思维

**第 3 步：进阶技巧**
- CoT 思维链
- Few-shot 示例
- ReAct 推理-行动模式`
  },
  'llm-app-guide.ts': {
    search: `LLM 应用开发的四个核心模块：

\`\`\`
┌─ 模块 1：LLM 原理 ──┐
│ • Token 机制        │
│ • 上下文窗口        │
│ • 温度参数          │
├─────────────────────┤
│ 模块 2：API 调用    │
│ • OpenAI API       │
│ • 成本估算和优化   │
├─────────────────────┤
│ 模块 3：RAG 系统    │
│ • 向量数据库       │
│ • Embedding        │
│ • 检索+生成        │
├─────────────────────┤
│ 模块 4：微调实战    │
│ • LoRA / QLoRA     │
│ • 开源模型适配     │
└─────────────────────┘
\`\`\``,
    replace: `LLM 应用开发的四个核心模块：

**模块 1：LLM 原理** — Token 机制、上下文窗口、温度参数

**模块 2：API 调用** — OpenAI API、成本估算和优化

**模块 3：RAG 系统** — 向量数据库、Embedding、检索+生成

**模块 4：微调实战** — LoRA / QLoRA、开源模型适配`
  },
  'agent-guide.ts': {
    search: `\`\`\`
  ┌──────────────┐
  │   感知模块   │  输入理解
  └──────┬───────┘
         ↓
  ┌──────────────┐
  │   规划模块   │  任务分解
  └──────┬───────┘
         ↓
  ┌──────┴───────┐
  │  工具执行 ←──┤ 记忆模块
  │  API/代码    │ 上下文保持
  └──────────────┘
\`\`\``,
    replace: `AI Agent 包含四大核心组件：

- **感知模块** — 理解用户输入和外部环境
- **规划模块** — 将复杂任务分解为可执行步骤
- **记忆模块** — 保持上下文和历史记录
- **工具执行** — 调用 API、执行代码、读写文件`
  },
  'aieng-guide.ts': {
    search: `\`\`\`
训练 → 优化 → 部署 → 监控 → 迭代
 │       │       │       │
 ▼       ▼       ▼       ▼
数据   量化    FastAPI  数据漂移
训练   剪枝    Docker   性能告警
实验   蒸馏    K8s     灰度发布
\`\`\``,
    replace: `AI 工程化涵盖五个阶段：

- **训练** — 数据准备、模型训练、实验管理
- **优化** — 量化、剪枝、蒸馏
- **部署** — FastAPI、Docker、Kubernetes
- **监控** — 数据漂移检测、性能告警
- **迭代** — 在线学习、A/B 测试、灰度发布`
  },
  'math-ml-guide.ts': {
    search: `\`\`\`
┌─ 数学基础 ────────────────┐
│ 线性代数  → 向量/矩阵    │
│ 概率论    → 分布/贝叶斯  │
│ 微积分    → 导数/梯度    │
├───────────────────────────┤
│ ML 基础                   │
│ 监督学习  → 回归/分类    │
│ 无监督    → 聚类/降维    │
├───────────────────────────┤
│ 进阶                      │
│ 决策树/SVM/随机森林      │
│ 特征工程/XGBoost         │
└───────────────────────────┘
\`\`\``,
    replace: `AI 基础学习分为三个层次：

**数学基础**
- 线性代数 → 向量、矩阵
- 概率论 → 分布、贝叶斯
- 微积分 → 导数、梯度

**ML 基础**
- 监督学习 → 回归、分类
- 无监督学习 → 聚类、降维

**进阶内容**
- 决策树、SVM、随机森林
- 特征工程、XGBoost`
  },
  'dl-guide.ts': {
    search: `\`\`\`
┌─ 神经网络基础 ──────────┐
│ 感知机→多层网络→反向传播│
│ 激活函数→优化器         │
├─────────────────────────┤
│ CNN（计算机视觉）       │
│ 卷积→池化→ResNet       │
├─────────────────────────┤
│ RNN / 序列模型          │
│ RNN→LSTM→注意力机制    │
├─────────────────────────┤
│ Transformer             │
│ Self-Attention→编码器   │
└─────────────────────────┘
\`\`\``,
    replace: `深度学习分为四个方向：

**神经网络基础** — 感知机、多层网络、反向传播、激活函数、优化器

**CNN（计算机视觉）** — 卷积、池化、ResNet

**RNN / 序列模型** — RNN、LSTM、注意力机制

**Transformer** — Self-Attention、编码器（LLM 的基石）`
  },
  'rl-guide.ts': {
    search: `\`\`\`
┌─ MDP（马尔可夫决策过程）─┐
│ 状态/动作/奖励/转移概率  │
├──────────────────────────┤
│ Q-Learning               │
│ 值函数/Bellman 方程      │
├──────────────────────────┤
│ 策略梯度                 │
│ REINFORCE/Actor-Critic  │
├──────────────────────────┤
│ DQN（深度 Q 网络）       │
│ 经验回放/目标网络        │
└──────────────────────────┘
\`\`\``,
    replace: `强化学习的学习路线：

**MDP（马尔可夫决策过程）** — 状态、动作、奖励、转移概率

**Q-Learning** — 值函数、Bellman 方程

**策略梯度** — REINFORCE、Actor-Critic

**DQN（深度 Q 网络）** — 经验回放、目标网络`
  },
  'cv-guide.ts': {
    search: `\`\`\`
┌─ 图像分类 ────────────┐
│ LeNet→AlexNet→ResNet │
├───────────────────────┤
│ 目标检测              │
│ YOLO/Faster R-CNN    │
├───────────────────────┤
│ 图像分割              │
│ 语义/实例分割        │
├───────────────────────┤
│ 数据增强与优化        │
│ AutoAugment/CutMix   │
└───────────────────────┘
\`\`\``,
    replace: `计算机视觉的学习路线：

**图像分类** — LeNet → AlexNet → ResNet

**目标检测** — YOLO、Faster R-CNN

**图像分割** — 语义分割、实例分割

**数据增强与优化** — AutoAugment、CutMix`
  },
  'mm-guide.ts': {
    search: `\`\`\`
┌─ CLIP（图文对齐） ─────┐
│ 对比学习/零样本分类   │
├───────────────────────┤
│ 视觉语言模型           │
│ Flamingo/BLIP/Qwen-VL│
├───────────────────────┤
│ 视频理解              │
│ 时序建模/动作识别     │
├───────────────────────┤
│ 多模态 Agent           │
│ 看图编程/视觉问答     │
└───────────────────────┘
\`\`\``,
    replace: `多模态学习的核心技术：

**CLIP（图文对齐）** — 对比学习、零样本分类

**视觉语言模型** — Flamingo、BLIP、Qwen-VL

**视频理解** — 时序建模、动作识别

**多模态 Agent** — 看图编程、视觉问答`
  },
  'genai-guide.ts': {
    search: `\`\`\`
┌─ VAE（变分自编码器）─┐
│ 编码→隐空间→解码    │
├─────────────────────┤
│ GAN（生成对抗网络） │
│ 生成器 vs 判别器    │
├─────────────────────┤
│ Diffusion Model     │
│ 加噪→去噪→生成     │
├─────────────────────┤
│ ControlNet          │
│ 精确控制生成结果    │
└─────────────────────┘
\`\`\``,
    replace: `生成式 AI 的技术全景：

**VAE（变分自编码器）** — 编码、隐空间、解码

**GAN（生成对抗网络）** — 生成器 vs 判别器

**Diffusion Model** — 加噪、去噪、生成

**ControlNet** — 精确控制生成结果`
  },
  'security-guide.ts': {
    search: `\`\`\`
┌─ 公平性与伦理 ─────────┐
│ 偏见检测/公平性度量   │
│ SHAP/LIME 可解释性    │
├───────────────────────┤
│ 隐私保护              │
│ 差分隐私/联邦学习     │
│ 安全多方计算          │
├───────────────────────┤
│ 对抗安全              │
│ 对抗样本/模型逆向     │
│ 对抗训练/输入消毒     │
└───────────────────────┘
\`\`\``,
    replace: `AI 安全分为三大维度：

**公平性与伦理** — 偏见检测、公平性度量、SHAP/LIME 可解释性

**隐私保护** — 差分隐私、联邦学习、安全多方计算

**对抗安全** — 对抗样本、模型逆向、对抗训练、输入消毒`
  }
};

for (const [file, { search, replace }] of Object.entries(replacements)) {
  let content = readFileSync(`${DIR}/${file}`, 'utf-8');
  if (content.includes(search)) {
    content = content.replace(search, replace);
    writeFileSync(`${DIR}/${file}`, content, 'utf-8');
    console.log(`✅ ${file} - replaced diagram with list`);
  } else {
    console.log(`⚠️  ${file} - pattern not found, skipping`);
  }
}

console.log('\nDone');
