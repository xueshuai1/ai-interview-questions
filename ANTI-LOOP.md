# 🛑 防循环与跳出机制 — ai-master.cc

> 这是铁律，不是建议。违反 = 白做。

---

## 🔴 死循环识别规则

### 规则 1：同一动作最多 2 次
- 同一个工具调用（如 exec grep 同样的内容）→ **最多 2 次**
- 第 2 次失败 → **立即换方案，不重试**
- 第 2 次结果和第 1 次相同 → **停止，换方向**

### 规则 2：输出重复检测
- 如果连续 2 次输出的内容相似度 > 80% → **立即停止**
- 例如：连续 2 次都在"分析历史数据"但没写代码 → 停

### 规则 3：进度检查
- 执行 3 分钟后自问：**"我产出了什么实际东西？"**
- 如果答案只是"我看了很多文件/跑了很多命令" → **立即停止分析，开始产出**

---

## 🚪 强制跳出机制

### 触发条件（满足任一即跳出）

| 条件 | 动作 |
|------|------|
| 同一工具调用 ≥ 2 次 | 换方案或停止 |
| 连续 2 次输出相似 | 停止当前任务 |
| 3 分钟无实际产出 | 跳到执行阶段 |
| 5 分钟未完成 | 汇报进度 + 停止 |
| 同一问题反复出现 ≥ 3 次 | 记录到 EVOLUTION.md，换方案 |

### 跳出后做什么

```
检测到循环 → 
  1. 记录到 EVOLUTION.md（什么问题、循环了几次）
  2. 换方案（如果有的话）
  3. 或直接停止 + 汇报（如果没有可行方案）
  4. 绝不重复第 3 次
```

---

## 📋 执行前检查清单

每次 cron 启动时自问：
1. 上次执行留下了什么未完成的事？
2. 上次遇到了什么问题？换方案了吗？
3. **我有没有在做和上次同样的事？** → 如果是，换方向

---

## 💀 历史教训（血泪经验）

| 日期 | 问题 | 循环次数 | 教训 |
|------|------|---------|------|
| 4/15-4/16 | GITHUB_TOKEN 失败 | 31 次 | 每次都"建议配置"，从未换方案 |
| 4/16-4/17 | Mermaid br 问题 | 17 次 | 反复修复反复出现 |
| 4/20 20:10 | 分析历史数据 | 15+ 次 | 反复 grep 同样的内容，没给出解决方案 |

**共同特征：都在"分析问题"或"建议方案"，但没有真正"换方案"或"解决问题"。**

---

## ✅ 正确做法示例

```
❌ 错误：
  执行失败 → 建议配置 → 失败 → 建议配置 → 失败 → 建议配置...（31 次）

✅ 正确：
  执行失败 → 建议配置 → 失败 → 换方案（改用 web_fetch）→ 
  web_fetch 也失败 → 再换（改用 GraphQL）→ 
  GraphQL 也失败 → 停止 + 记录 + 汇报
  （最多 3 次尝试，绝不循环）
```

---

_这个文档是 cron 执行时必须遵守的铁律。_
_违反 = 白做。_
